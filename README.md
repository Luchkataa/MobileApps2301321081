# 🍺 BrewTracker: Проследяване и Оценяване на Крафт Напитки

## 💡 Идея на Проекта

BrewTracker е мобилно Android приложение, разработено да служи като **персонална база данни** за ентусиасти на крафт напитки. То позволява на потребителите лесно да регистрират, оценяват (по рейтинг) и запазват подробни бележки за всяка напитка, която консумират.

## ⚙️ Как Работи

Приложението следва **MVVM архитектурата** за чиста и поддържаема кодова база. Потребителският интерфейс (View) взаимодейства с **ViewModel**, който съхранява данните. ViewModel комуникира с **Repository**, който абстрахира източниците на данни. За перманентно съхранение се използва **Room Persistence Library**, работеща върху локална SQLite база данни. Всички промени в данните (CRUD операции) се извършват асинхронно чрез `ExecutorService`, а резултатите се предават обратно към UI чрез **LiveData**, осигурявайки реактивна актуализация на списъка. Този дизайн гарантира, че приложението е стабилно и лесно за тестване.

## 🏗️ Архитектура

Приложението е изградено върху принципите на **Model-View-ViewModel (MVVM)**, използвайки следните основни компоненти на Android Architecture Components:

* **View:** `MainActivity` (RecyclerView) и `NewBrewActivity` (Форма за вход).
* **ViewModel:** Управлява данните за UI и преживява промени в конфигурацията.
* **Repository:** Посредник между ViewModel и източника на данни.
* **Model:** **Room** (за база данни), **BrewDao** (за достъп до данни), **Brew** (Entity клас).
* **Reactive Data Flow:** Използване на **LiveData** за автоматично актуализиране на потребителския интерфейс при промяна на базата данни.

## 🔄 Потребителски Поток (User Flow)

1.  **Преглед:** Потребителят вижда списък с всички запазени напитки на главния екран (`MainActivity`).
2.  **Създаване:** Натискане на Плаващия Бутон за Действие (FAB) стартира `NewBrewActivity`.
3.  **Въвеждане:** Потребителят въвежда Име, Оценка и Бележки.
4.  **Записване:** Натискане на "Save" стартира `insert()` операцията в Repository-то и актуализира списъка.
5.  **Управление:** Дълго натискане върху елемент от списъка отваря контекстуално меню за **Редактиране** или **Изтриване** на елемента.

## 🛠️ Стъпки за Стартиране

1.  **Клониране:** Клонирайте репозиторито:
    ```bash
    git clone [ВЪВЕДИ АДРЕСА НА ТВОЯ РЕПОЗИТОРИ]
    ```
2.  **Синхронизация:** Отворете проекта в Android Studio и изчакайте Gradle да синхронизира зависимостите.
3.  **Изпълнение:** Стартирайте приложението на Android емулатор (API 29+ е препоръчителен) или физическо устройство.

## 🧪 Тестови Акаунти

Тъй като приложението използва локална база данни (Room), **не са необходими тестови акаунти**. Всички данни се създават и съхраняват локално на устройството.

## 🔗 Линк към APK Файла

Готовата, подписана версия на приложението е достъпна за директно изтегляне и инсталиране:

[Изтегли app-release.apk]
